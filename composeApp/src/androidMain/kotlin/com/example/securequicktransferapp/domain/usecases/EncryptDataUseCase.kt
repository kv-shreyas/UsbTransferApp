package com.example.securequicktransferapp.domain.usecases

import com.example.securequicktransferapp.data.crypto.CryptoManager
import javax.crypto.SecretKey
import javax.inject.Inject

/*
class EncryptDataUseCase @Inject constructor(
    private val crypto: CryptoManager
) {
    operator fun invoke(data: ByteArray, key: SecretKey):Pair<ByteArray, ByteArray>
        = crypto.encrypt(data, key)
}*/
